{
  "name": "filevault",
  "version": "1.0.0",
  "description": "FileVault - Organizator de fișiere cu Google Drive",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Static PWA - no build step needed'",
    "android": "npx cap open android",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
